{
    "name": "fornecedor-analise",
    "version": "1.0.0",
    "scripts": {
        "build": "cd FornecedorFlow && npm install --include=dev && npm run build",
        "start": "cd FornecedorFlow && npm start"
    }
}